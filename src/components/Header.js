import React from "react";
import Nav from "./Nav";

const Header = () => {
  return (
    <header className="Header">
      <div>PokeApp</div>
      <Nav />
    </header>
  );
};

export default Header;
